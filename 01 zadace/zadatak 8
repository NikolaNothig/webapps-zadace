const obj1 = {a:1, b:2, c:3}
const obj2 = {d:321, e:3, f:1}

const jednako = (obj1, obj2) => {
    const obj1keys = Object.keys(obj1);
    const obj2keys = Object.keys(obj2);

    for (let objkey of obj1keys) {
        if(obj1[objkey] !== obj2[objkey]){
        return false
    }
}
return true
}

jednako(obj1,obj2);
